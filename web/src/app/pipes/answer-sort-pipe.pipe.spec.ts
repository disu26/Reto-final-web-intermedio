import { AnswerSortPipePipe } from './answer-sort-pipe.pipe';

describe('AnswerSortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AnswerSortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
